package calculatorAPP;

import java.util.Scanner;
import java.util.ArrayList;
import java.util.List;

public class Runner {

	public static void main(String[] args) {
		
		Domain input = new Domain()	;
		
		input.inputMethod();
		
		
//		System.out.println(validation.isValidNumbers());
		
//		if (!validation.isValidNumber(firstNumber) || !validation.isValidNumber(secondNumber)) {
////			throw new IllegalArgumentException("Invalid input, Please Try Again");
//		}
		
		

		// Scanner scanner = new Scanner(System.in);

//		System.out.print("Enter the first number: ");
//		String firstNumber = scanner.next();
//
//		System.out.print("Enter the second number: ");
//		String secondNumber = scanner.next();
//
//				scanner.close(); 

//
//		if (!validation.isValidNumber(firstNumber) || !validation.isValidNumber(secondNumber)) {
//			throw new IllegalArgumentException("Invalid input, Please Try Again");
//		}
//		double operand1 = Double.parseDouble(firstNumber);
//		double operand2 = Double.parseDouble(secondNumber);
//
//		Calculator calculator = new Calculator();
//
//	}

	}
}
